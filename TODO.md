# TODO
- [x] Fix ReferenceError in `src/controllers/task.controller.js` by renaming imported model to `Task` and created document to `createdTask`.
- [x] Fix request field mismatch `catagory` -> `category` and ensure created task uses `category`.
- [ ] Re-run the server to verify no ReferenceError occurs.


